---
phase: 01-stability-fixes-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/utils/game/getWarOutcome.mjs
  - src/__tests__/unit/shared/utils/game/getWarOutcome.test.mjs
  - src/__tests__/unit/features/archives/buildWarNarrative.test.mjs
autonomous: true
requirements: [STAB-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "getWarOutcome returns a result instead of throwing when a faction slot in data.status is null (STAB-05, D-19)"
    - "getWarOutcome returns a result instead of throwing when a faction slot inside snapshots[].data is null (STAB-05, D-19)"
    - "buildPlayerBeats returns an empty beat list when the median player baseline is zero, and that behaviour is pinned by a test (D-19)"
    - "The four-command project verification chain (lint, typecheck, test:unit, build) passes on the working branch after the fix lands"
    - statement: "No other call site in src/shared/utils/game/ regresses because of the null guard — the existing 11 getWarOutcome cases still pass unchanged"
      verification: backstop
  artifacts:
    - path: "src/shared/utils/game/getWarOutcome.mjs"
      provides: "Null-tolerant victory-signal checks at both .every call sites"
      contains: "CAMPAIGN_STATUS.DEFEATED"
    - path: "src/__tests__/unit/shared/utils/game/getWarOutcome.test.mjs"
      provides: "Regression cases for a null slot in live status and in snapshot data"
      contains: "null"
    - path: "src/__tests__/unit/features/archives/buildWarNarrative.test.mjs"
      provides: "Zero-baseline fixture case for the player-beats guard"
      contains: "buildWarNarrative"
  key_links:
    - from: "src/features/archives/buildWarNarrative.mjs"
      to: "src/shared/utils/game/getWarOutcome.mjs"
      via: "getWarOutcome(data) called from the narrative builder with raw archive snapshots"
      pattern: "getWarOutcome"
    - from: "src/features/ministry/warTone.mjs"
      to: "src/shared/utils/game/getWarOutcome.mjs"
      via: "getWarOutcome(data) called from the war-tone resolver"
      pattern: "getWarOutcome"
---

<objective>
Fix `getWarOutcome`'s unguarded faction-slot access at root cause and pin it with regression tests, then prove the whole phase loop end to end: source fix -> mirrored unit test -> four-command verification chain -> atomic commit.

This is the phase's **tracer slice**. Every other plan in Phase 1 repeats this exact loop against a different bug class. Running it first, on the smallest bug, proves the loop works before four more fixes are layered on top of it.

Purpose: STAB-05 (#459) — archive and narrative pages read historical snapshots directly, bypassing `getCampaign()`'s guards, so a season with a null faction slot currently 500s the page.
Output: A null-tolerant `getWarOutcome`, two new regression cases, one new `buildPlayerBeats` zero-baseline case, all green under `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run build`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stability-fixes-release/01-CONTEXT.md
@.planning/phases/01-stability-fixes-release/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end null-slot tolerance in getWarOutcome — one bug class, fixed and pinned</name>
  <precondition>The working tree is on a branch that is neither `main` nor `develop`, and `.planning/phases/01-stability-fixes-release/` exists in that tree (all Phase 1 fix commits accumulate on this one branch; plan 01-08 merges it into `develop`).</precondition>
  <read_first>
    - src/shared/utils/game/getWarOutcome.mjs (the module being changed; read the full JSDoc algorithm block at the top before editing — it is the contract this fix must keep accurate)
    - src/__tests__/unit/shared/utils/game/getWarOutcome.test.mjs (the existing 11-case suite; match its fixture-literal assertion style exactly — plain object fixtures, `expect(result.outcome).toBe(...)`)
    - .planning/codebase/CONCERNS.md (section "getWarOutcome crashes on null faction slots — #459" for the reachability argument)
    - src/shared/enums/events.mjs (the CAMPAIGN_STATUS / EVENT_STATUS / EVENT_TYPE values the fixtures must use)
  </read_first>
  <files>src/shared/utils/game/getWarOutcome.mjs, src/__tests__/unit/shared/utils/game/getWarOutcome.test.mjs</files>
  <behavior>
    - `getWarOutcome({ status: [{status:'defeated'}, null, {status:'defeated'}], snapshots: [], events: [] })` returns an object, does not throw, and does not report victory (a null slot is not a defeated faction).
    - `getWarOutcome({ status: [...3 active...], snapshots: [{ data: [{status:'defeated'}, null, {status:'defeated'}] }], events: [] })` returns an object, does not throw, and that snapshot does not satisfy the all-defeated signal.
    - All 11 pre-existing cases keep their current results byte-for-byte.
  </behavior>
  <action>
    Per D-19, make both victory-signal checks tolerate a null entry instead of dereferencing it.

    Two call sites in `getWarOutcome`:
    1. The live-status check `live.length === 3 && live.every((f) => f.status === CAMPAIGN_STATUS.DEFEATED)`.
    2. The snapshot check inside `snapshots.some(...)`, where `factionData.every((f) => f.status === CAMPAIGN_STATUS.DEFEATED)` runs after the `Array.isArray` and `length === 3` guards.

    Apply optional-chained access (`f?.status`) at both sites, or an equivalent early bail that rejects the array when any entry is falsy. Either shape is acceptable; pick one and use the same shape at both sites so the two checks stay symmetric. The semantic requirement is fixed: a null slot must make the all-defeated signal FALSE, never true and never a throw. Do not reorder or otherwise alter the three victory signals, the defeat signal, or the faction-attribution logic.

    Then extend the JSDoc algorithm block at the top of the file with one line under the victory-signals list recording that a null or missing faction slot fails the all-defeated check rather than throwing, so the "verified against 137 wiki-confirmed seasons" claim stays accurate about which inputs are handled.

    Finally add the two regression cases described in `<behavior>` to the existing suite, following its fixture-literal style. Name them so the null slot is visible in the test title.
  </action>
  <verify>
    <automated>npm run test:unit -- src/__tests__/unit/shared/utils/game/getWarOutcome.test.mjs</automated>
    <fails_when>non-zero exit, or the summary shows "Test Files  1 failed", or stderr contains "No test files found", or the "Tests" summary line reports fewer than 13 passed</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or any line matching "error" in the ESLint report</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>npm run build</automated>
    <fails_when>non-zero exit, or output contains "Failed to compile"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/shared/utils/game/getWarOutcome.mjs` contains no bare `f.status` inside either `.every` callback; both callbacks reach `status` through an optional chain or behind an explicit falsy-entry bail.
    - The JSDoc block at the top of `getWarOutcome.mjs` names the null-slot behaviour in at least one line.
    - `npm run test:unit -- src/__tests__/unit/shared/utils/game/getWarOutcome.test.mjs` reports at least 13 passing tests (11 pre-existing + 2 new) and 0 failing.
    - A run with the null-slot fixtures produces `outcome: 'defeat'` (no victory signal) rather than an exception — assert the returned object, not just the absence of a throw.
    - All four project commands (`npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run build`) exit 0.
    - The change is committed as one atomic commit on the working branch with a `fix(01-01):` prefixed message.
  </acceptance_criteria>
  <done>A null faction slot in either live status or snapshot data yields a normal `getWarOutcome` result instead of a TypeError, two regression cases pin it, and the four-command chain is green on the committed tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin buildPlayerBeats' zero-baseline guard with a fixture test</name>
  <read_first>
    - src/features/archives/buildWarNarrative.mjs (lines 29-80 — the `buildPlayerBeats` function, its `series.length < 2` and `baseline <= 0` guards, and the `median` helper it depends on; also line ~354 where the narrative builder consumes it)
    - src/__tests__/unit/features/archives/buildWarNarrative.test.mjs (the existing suite; `buildPlayerBeats` is module-private, so the new case must drive it through the exported narrative builder using a `playerTimeseries` fixture — follow how the existing cases shape their input)
  </read_first>
  <files>src/__tests__/unit/features/archives/buildWarNarrative.test.mjs</files>
  <behavior>
    - Test 1 (zero baseline): a `playerTimeseries` of >= 2 samples whose median `total` is 0 produces no surge beat and no collapse beat in the narrative output.
    - Test 2 (single sample): a `playerTimeseries` of exactly 1 sample produces no player beats — the `series.length < 2` guard.
  </behavior>
  <action>
    Per D-19, `buildPlayerBeats`' zero-baseline guard is currently untested. Add the two cases in `<behavior>` to the existing `buildWarNarrative` suite.

    `buildPlayerBeats` is not exported, so drive it through the module's public entry point with a `data.playerTimeseries` fixture shaped `{ time, day, total }` per its JSDoc. Assert on the absence of surge/collapse beats in the returned narrative — identify the beat marker from the surrounding narrative-assembly code and the `PHRASES.surge` / `PHRASES.collapse` phrasing keys rather than matching on rendered prose, so the assertion survives the #453 phrasing-variety work in a later phase.

    Do not change `buildWarNarrative.mjs` in this task. This is coverage for an existing guard, not a behaviour change. If the zero-baseline case reveals a real defect rather than a covered guard, stop and report it rather than widening the fix.
  </action>
  <verify>
    <automated>npm run test:unit -- src/__tests__/unit/features/archives/buildWarNarrative.test.mjs</automated>
    <fails_when>non-zero exit, or the summary shows "Test Files  1 failed", or stderr contains "No test files found"</fails_when>
    <automated>npm run test:unit</automated>
    <fails_when>non-zero exit, or the summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/unit/features/archives/buildWarNarrative.test.mjs` gains exactly two cases whose titles name the zero-baseline and single-sample conditions.
    - Both new cases assert the *absence* of surge and collapse beats — not merely that the call returns without throwing.
    - `src/features/archives/buildWarNarrative.mjs` is unchanged (`git diff --name-only` for this commit lists only the test file).
    - The full `npm run test:unit` run is green.
    - Committed as one atomic commit with a `test(01-01):` prefixed message.
  </acceptance_criteria>
  <done>The zero-baseline and single-sample paths through `buildPlayerBeats` are covered by tests that fail if either guard is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres `h1_status` / `h1_season` rows -> `getWarOutcome` | Historical snapshot rows reach the archive/narrative render path directly, without `getCampaign()`'s shape guards |
| Archive page request -> narrative builder | An unauthenticated visitor picks the season number, choosing which stored snapshot shape is deserialised |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | `src/shared/utils/game/getWarOutcome.mjs` victory-signal checks | medium | mitigate | Null-tolerant `.every` callbacks at both call sites (Task 1) remove the unauthenticated visitor's ability to 500 an archive page by requesting a season whose stored snapshot has a null faction slot |
| T-01-02 | Tampering | Snapshot rows read outside `getCampaign()`'s guards | low | accept | Rows are written only by the key-gated `/api/h1/update` poller from Zod-validated upstream payloads; no external write path exists to this table, so shape tampering is not reachable at ASVS L1 |
| T-01-03 | Information Disclosure | Thrown TypeError surfacing a stack frame to the visitor | low | mitigate | The null guard removes the throw entirely; the route-level `error.jsx` boundary already prevents raw stack disclosure on any residual failure |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions (edge probe — spec-less fallback)

- **STAB-05 / unclassified — unresolved.** The edge probe could not classify STAB-05 into an edge category ("unclassified — review manually"). Recorded here rather than dropped. Reviewed manually during planning: the requirement text ("tolerates null faction slots; regression test pins it") is already a testable acceptance criterion, and Task 1's behaviour block encodes it for both call sites. No additional edge predicate is proposed. If a reviewer identifies a category (likely `empty` — what is the result for an empty or single-element `status` array?), the existing `live.length === 3` and `Array.isArray`/`length === 3` guards already cover it and the suite's `returns null when no data` case pins the empty end.
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Symbol / path | Kind | New or modified |
|---|---|---|
| `src/shared/utils/game/getWarOutcome.mjs` | module (null-tolerant victory-signal checks) | modified |
| JSDoc null-slot line in `getWarOutcome.mjs` algorithm block | documentation | new |
| Null-slot regression case (live `status`) in `getWarOutcome.test.mjs` | test case | new |
| Null-slot regression case (snapshot `data`) in `getWarOutcome.test.mjs` | test case | new |
| Zero-baseline `buildPlayerBeats` case in `buildWarNarrative.test.mjs` | test case | new |
| Single-sample `buildPlayerBeats` case in `buildWarNarrative.test.mjs` | test case | new |
</artifacts_this_phase_produces>

<verification>
- `npm run lint` exits 0.
- `npm run typecheck` exits 0.
- `npm run test:unit` exits 0 with no "failed" in the summary line.
- `npm run build` exits 0.
- `git log --oneline` on the working branch shows two new commits prefixed `fix(01-01):` and `test(01-01):`.
</verification>

<success_criteria>
STAB-05 is closed at root cause: a null faction slot in either input path produces a normal result, two regression cases fail if the guard is removed, `buildPlayerBeats`' zero-baseline guard is covered, and the phase's fix -> test -> verify -> commit loop is proven end to end before any other bug class is touched.
</success_criteria>

<output>
Create `.planning/phases/01-stability-fixes-release/01-01-SUMMARY.md` when done.
</output>
